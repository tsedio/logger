import {expect} from "../../tools";
import {pad, truncate} from "../../../src/layouts/utils/StringUtils";

describe("StringUtils", () => {
  describe("truncate()", () => {
    it("should", () => {
      // expect(truncate("truncation", "replacement")).to.eq("");
    });
  });

  describe("pad()", () => {
    it("should", () => {
      // expect(pad("truncation", "replacement")).to.eq("");
    });
  });
});
